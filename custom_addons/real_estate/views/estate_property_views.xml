
<odoo>

    <!-- Form view-->
    <record id="real_estate_property_form_view" model="ir.ui.view">
        <field name="name">real_estate.property.view.form</field>
        <field name="model">real_estate.property</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <group>
                        <button name="action_cancel" type="object" string="Cancel" class="btn btn-primary" />
                        <button name="action_sold" type="object" string="Sold" class="btn btn-primary" />
                    </group>
                    
                    <group>
                        <field name="status" string="" widget="statusbar" status_visible="active,sold" />
                    </group>
                </group>
                
                <sheet>
                    <group>
                        <h1>
                            <field name="name" placeholder="Enter property name"/>
                        </h1>
                    </group>

                    <group>
                        <group>
                            <field name="tag_ids" widget="many2many_tags" />
                        </group>
                    </group>

                    <group>
                        <group>
                            <field name="status" readonly="1"/>
                            <field name="property_type_id" />
                            <field name="postcode" placeholder="Enter postcode" />
                            <field name="date_availability" />
                        </group>
                        
                        <group>
                            <field name="expected_price" placeholder="Enter expected price"/>
                            <field name="best_offer"/>
                            <field name="selling_price" readonly="1"/>
                        </group>
                    </group>

                    <group>
                        <notebook class="o_notebook_bordered">
                            <page string="Description">
                                <group>
                                    <field name="bedrooms" />
                                    <field name="living_area" string="Living area (sqm)" />
                                    <field name="facades" />
                                    <field name="garage" />
                                    <field name="garden" />
                                    <field name="garden_area" string="Garden area (sqm)" options="{'readonly':[('garden', '=', False)]}" />
                                    <field name="garden_orientation" />
                                    <field name="total_area" string="Total area (sqm)" />
                                </group>
                            </page>

                            <page string="Computed dates (Temporary)">
                                <group>
                                    <field name="validity" />
                                    <field name="date_deadline" />
                                </group>
                            </page>

                            <page string="Offers">
                                <field name="offer_ids">
                                    <list editable="bottom">
                                        <field name="partner_id"/>
                                        <field name="price"/>
                                        <button icon="fa-check" title="Accept offer" type="object" name="action_accept" />
                                        <button icon="fa-times" title="Refuse offer" type="object" name="action_refuse" /> 
                                        <field name="status"/>
                                    </list>
                                </field>
                            </page>

                            <page string="Other Info">
                                <group>
                                    <field name="seller_id" />
                                    <field name="buyer_id" readonly="1" />
                                </group>
                            </page>
                        </notebook>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- List View -->
    <record id="real_estate_property_tree_view" model="ir.ui.view">
        <field name="name">real_estate.property.view.list</field>
        <field name="model">real_estate.property</field>
        <field name="arch" type="xml">
            <list default_order="create_date desc, name">
                <field name="name"/>
                <field name="status"/>
                <field name="create_date" options="{'readonly': True}"/>
                <field name="number_of_offers"/>
                <field name="tag_ids" widget="many2many_tags"/>
                <field name="date_availability"/>
                <field name="expected_price"/>
                <field name="selling_price" readonly="1"/>
                <field name="property_type_id"/>
            </list>
        </field>
    </record>

    <!-- Action -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Real Estate</field>
        <field name="res_model">real_estate.property</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
